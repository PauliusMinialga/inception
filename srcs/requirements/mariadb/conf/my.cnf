# MariaDB custom configuration
#
# This file overrides the default MariaDB settings to allow
# connections from other Docker containers.

[mysqld]
# The default is 127.0.0.1 (localhost), which blocks external connections.
# 0.0.0.0 tells MariaDB to listen on all available network interfaces.
bind-address = 0.0.0.0 
